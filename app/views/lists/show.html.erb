<h1> This is <%= @list.name %> </h1>


<section class="todoapp">
			<header class="header">
				<h1><%= @list.name %> </h1>
                <%= form_for([@list, @item]) do |f| %>
                 <%= f.text_field :description, :class => "new-todo", :placeholder => (@item.errors.full_messages_for(:description).first || "Enter a new name") %> 
				<%# form_for(@item, :url => list_items_path(@list)) do |f| %>
                <%#form action="/lists" method="POST">
                <%# raise form_for(Item.new, :url => list_items_path(@list)) do |f| %>
                    <%# f.text_field :description, :class => "new-todo" %>
                    <%= f.submit :style => "display:none"%>
                <% end %>
                </form>
			</header>
			<section class="main">
                    <input class="toggle-all" type="checkbox">
                    <label for="toggle-all"> Mark ask complete </label>
                <ul class="todo-list">
                    <% @list.items.each do |item| %>
                    <li class="<%= "completed" if item.complete? %>">
                        <div class="view">
                            <%# raise list_item_path(@list, item).inspect %>
                            <%# raise list.inspect if item.id.nil?%>
                            <%# form_for([@list, item], :url => list_item_path(@list, item), :method => :put) do |f| %>
                            <%# f.check_box :status, :class => "toggle", :checked => (true if item.complete?) %>
                            <%# f.submit %>

                        <%# form_for([@list, item], :url => list_item_path(@list, item), :method => :put) do |f| %>   
                        <%# f.submit%>
                        <%# end %>

                         <%= form_for([@list, item]) do |f| %>
                            <%= f.check_box :status, :class => "completed", :checked => (true if item.complete?) %>
                            <%= f.submit%>
                            <% end %>

             
                            <label><%= item.description %></label>
                        </div>
                    </li>
                    <%end %>
                </ul>
			</section>
		</section>